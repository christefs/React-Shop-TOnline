import React from 'react';
import '../styles/LabelPass.scss';

const LabelPass = () => {
    return (

        <label for="password" className="label">Password</label>

    );
}

export default LabelPass;